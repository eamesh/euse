import { SvgIcon, SvgIconProps } from '@mui/material';

import EuseIcon from './images/euse.svg';

export default function Keys(props: SvgIconProps) {
  return <SvgIcon component={EuseIcon} viewBox="0 0 150 58" {...props} />;
}
